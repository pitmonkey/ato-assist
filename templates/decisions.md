# Decisions

Append-only. One entry per judgement call, newest at the bottom. `ato status` reads the headings, so keep the `## YYYY-MM-DD — title` form exactly.

Record the call, not the deliberation: what was decided, why, and who decided it.
