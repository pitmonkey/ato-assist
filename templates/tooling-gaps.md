# Tooling gaps

Every time something was handled by hand because no adapter existed, it goes here. This file is the adapter backlog: the entries that recur are the adapters worth building.

`ato status` counts the open headings, so keep the `## YYYY-MM-DD — title` form exactly. Close a gap by editing its entry to say what now handles it.
